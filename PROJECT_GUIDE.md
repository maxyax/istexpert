# 🚀 ISTExpert.ru - Руководство по проекту

## 📍 Расположение проекта

**Локальная папка:** `/Users/maksimakusev/istexpert/`
**GitHub:** https://github.com/maxyax/istexpert
**Продакшн:** https://istexpert.ru (автодеплой через Vercel)

---

## 🛠 Как начать работу

### 1. Открыть проект в Cursor:
```bash
cd ~/istexpert
cursor .
```

### 2. Запустить локальный сервер:
```bash
npm run dev
```
Сайт откроется на: http://localhost:5173

### 3. Внести изменения:
- Использовать Composer (Cmd+I) для описания задачи
- Cursor автоматически изменит код
- Проверить на localhost:5173

### 4. Задеплоить на продакшн:
```bash
git add .
git commit -m "Описание изменений"
git push
```
Vercel автоматически задеплоит на istexpert.ru через 2-3 минуты

---

## 📦 Технологический стек

- **Frontend:** React 19 + TypeScript
- **Сборка:** Vite
- **Стили:** Tailwind CSS (Neomorphism дизайн)
- **State Management:** Zustand
- **Иконки:** Lucide React
- **QR коды:** qrcode
- **Деплой:** Vercel
- **Репозиторий:** GitHub

---

## 📂 Структура проекта

```
istexpert/
├── pages/                    # Страницы приложения
│   ├── EquipmentList.tsx    # ⭐ ГЛАВНЫЙ ФАЙЛ - Автопарк
│   ├── Dashboard.tsx        # Дашборд
│   ├── Maintenance.tsx      # ТО и ремонт
│   ├── Landing.tsx          # Главная страница
│   └── Login.tsx            # Авторизация
├── store/                   # Zustand хранилища
│   ├── useFleetStore.ts     # Управление автопарком
│   ├── useMaintenanceStore.ts
│   └── useAuthStore.ts
├── components/              # Компоненты
│   └── Layout.tsx
├── .env                     # Переменные окружения
├── package.json
└── README.md
```

---

## ✅ Реализованные функции (последнее обновление)

### 🚗 Автопарк (EquipmentList.tsx)

1. **Добавление техники** ✅
   - Кнопка + создает новую технику
   - Автоматически открывается в режиме редактирования

2. **Удаление техники** ✅
   - Кнопка удаления на всех вкладках (Основное, Документы, История)
   - Модальное окно подтверждения

3. **Редактирование полей** ✅
   - Марка (отдельное поле)
   - Модель (отдельное поле)
   - Название формируется автоматически: Марка + Модель
   - Гос. номер
   - VIN
   - Год выпуска
   - Наработка/пробег (редактируемое, с единицами "м/ч")
   - Водитель
   - **Контрагент/Продавец** ✅
   - **Телефон контрагента** ✅
   - **Email контрагента** ✅

4. **QR код** ✅
   - Генерируется автоматически для каждой техники
   - Содержит полную ссылку: `${window.location.origin}/?equipment=${id}`
   - При сканировании открывает карточку техники
   - Размер: 300px

5. **Загрузка фото** ✅
   - Кнопка Camera в режиме редактирования
   - Поддержка всех форматов изображений (image/*)
   - Конвертация в base64
   - Сразу отображается в карточке

6. **Документы** ✅
   - 6 типов документов:
     1. СТС / ПТС
     2. Страховка ОСАГО
     3. Диагностическая карта
     4. **Каталог** ✅ (новый)
     5. **Инструкция** ✅ (новый)
     6. **Другое** ✅ (новый)
   - Загрузка документов (PDF/DOC/DOCX/TXT)
   - Кнопка Upload в режиме редактирования
   - Просмотр PDF в новой вкладке при клике

7. **Улучшенный UI** ✅
   - Кнопка закрытия (X) - красная, 24px, хорошо видна на мобильных
   - Адаптивные отступы для мобильных устройств
   - Neomorphism дизайн

---

## 🔧 Важные функции в коде

### useFleetStore.ts
```typescript
- addEquipment(equipment)      // Добавить технику
- deleteEquipment(id)          // Удалить технику
- updateEquipment(id, data)    // Обновить технику
- selectEquipment(id)          // Выбрать технику
```

### EquipmentList.tsx - Ключевые функции
```typescript
- handleAddNew()               // Создать новую технику
- handleDelete()               // Удалить с подтверждением
- handleSave()                 // Сохранить изменения
- handleUploadDocument()       // Загрузить документ
- handleUploadPhoto()          // Загрузить фото
```

---

## 🎯 Workflow разработки

### Быстрый цикл:
```
1. Открыть Cursor
2. Cmd+I → описать задачу
3. Код автоматически меняется
4. npm run dev → проверить на localhost:5173
5. Если ОК → git add . && git commit -m "..." && git push
6. Vercel деплоит на istexpert.ru
7. Проверить на продакшене
```

### Если нужны правки:
```
1. Cmd+I → описать что исправить
2. Повторить цикл
```

---

## 📝 История изменений

### Последний коммит (18.02.2026):
**"Add 12 new features: delete equipment, upload docs, improved close button, add equipment, editable hours, supplier fields, auto equipment name, QR code with link, photo upload, 3 new doc types, PDF viewer"**

Изменены файлы:
- `pages/EquipmentList.tsx` - все основные функции
- `.env` - переменные окружения

### Что было добавлено:
1. ✅ Кнопка + для добавления техники
2. ✅ Удаление техники с подтверждением
3. ✅ Поля контрагента (Продавец, Телефон, Email)
4. ✅ Разделение Марки и Модели
5. ✅ Автоматическое название из Марки + Модели
6. ✅ Редактируемое поле наработки с единицами
7. ✅ QR код со ссылкой на карточку
8. ✅ Загрузка фото через Camera
9. ✅ 3 новых типа документов
10. ✅ Просмотр PDF в новой вкладке
11. ✅ Улучшенная кнопка закрытия для мобильных
12. ✅ Загрузка документов

---

## 🚨 Важные заметки

### ⚠️ Какой файл редактировать:
**ВСЕГДА редактируйте:** `pages/EquipmentList.tsx`
**НЕ редактируйте:** `EquipmentList.tsx` (в корне) - это старая версия!

### ⚠️ Gemini AI:
Gemini AI **НЕ используется** в коде! Упоминается только в документации.
Замена на OpenRouter не требуется.

### ⚠️ Локальный сервер:
После изменений в коде, сервер автоматически перезагружается.
Если не работает - перезапустите: `Ctrl+C` → `npm run dev`

---

## 🔮 Что установлено на ноутбуке

### ✅ Установлено:
- Node.js + npm
- Cursor (IDE)
- Git
- Docker
- Ollama (с моделью deepseek-coder:1.3b - 776 MB)

### ❌ Удалено:
- WebUI (Docker образ ~6GB)
- qwen2.5-coder:7b (4.7GB)

---

## 💡 Советы для эффективной работы

1. **Всегда тестируйте локально** перед пушем на GitHub
2. **Используйте Cmd+I в Cursor** для быстрых изменений
3. **Коммиты с понятными сообщениями** - легче откатиться
4. **Проверяйте на мобильных** - адаптивность важна
5. **Сохраняйте резервные копии** важных изменений

---

## 🆘 Частые проблемы

### Проблема: Изменения не видны на localhost
**Решение:** Проверьте что редактируете `pages/EquipmentList.tsx`, а не корневой файл

### Проблема: Ошибка компиляции
**Решение:** Проверьте закрытие всех тегов JSX, импорты компонентов

### Проблема: Git push не работает
**Решение:** 
```bash
git status  # проверить статус
git add .   # добавить все файлы
git commit -m "message"  # закоммитить
git push    # запушить
```

### Проблема: Vercel не деплоит
**Решение:** Проверьте статус на vercel.com, возможно ошибка сборки

---

## 📞 Контакты и ссылки

- **Сайт:** https://istexpert.ru
- **GitHub:** https://github.com/maxyax/istexpert
- **Vercel:** https://vercel.com/dashboard
- **Локальный проект:** `/Users/maksimakusev/istexpert/`

---

**Последнее обновление:** 18 февраля 2026, 16:12
**Версия документа:** 1.0
