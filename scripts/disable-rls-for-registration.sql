-- ISTExpert: Временное отключение RLS для регистрации
-- Выполните этот SQL в Supabase SQL Editor

-- ============================================
-- ВАРИАНТ 1: Полностью отключить RLS для companies
-- ============================================

-- Это позволит создавать компании без ограничений
-- После тестов можно включить обратно
ALTER TABLE companies DISABLE ROW LEVEL SECURITY;

-- ============================================
-- ВАРИАНТ 2: Или создайте супер-политику для анонимов
-- ============================================

-- Если хотите оставить RLS включённым, создайте политику:
-- ALTER TABLE companies ENABLE ROW LEVEL SECURITY;
--
-- CREATE POLICY "allow_all_for_companies"
--   ON companies FOR ALL
--   TO authenticated
--   USING (true)
--   WITH CHECK (true);

-- ============================================
-- После выполнения попробуйте зарегистрироваться снова
-- ============================================
